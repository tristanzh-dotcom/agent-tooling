# Global Model Routing Record

Last updated: 2026-06-04

Scope: all Codex-managed projects under `/Users/tristanzh/agent`.

This file is the shared startup record for model routing, privacy boundaries, and first-principles usage rules. Every project should read this file before making assumptions about model responsibility.

## First-Principles Rule

Facts are not generated by language models.

For sales, rankings, YTD totals, brand/model quantities, schedules, source URLs, and other factual values:

- use deterministic parsers, source-specific collectors, cached verified data, and validation rules;
- never accept an LLM output as the final factual source;
- if an LLM is used to parse or normalize text, treat its output as a candidate that must pass schema, range, period, and source checks;
- mark uncertain or structurally incomplete data with the existing `（待查）` convention.

LLMs may be used for:

- Chinese semantic understanding;
- Chinese wording and summarization;
- translation or text normalization;
- workflow assistance where the output is not itself a factual number.

## Current Project Routes

| Project | Route Key | Provider | Model / Policy | Privacy Status | Responsibility |
|---|---|---|---|---|---|
| `中国乘用车销量汇总` / `项目2.1` | `fact_layer` | none | `deterministic_rules_only` | local deterministic processing | Sales, rankings, YTD, brand/model quantities are confirmed only by rules and source validation. |
| `中国乘用车销量汇总` / `项目2.1` | `core` | `GPT5_5` | `gpt-5.5-flash` | API request if invoked | Workflow metadata, orchestration semantics, and Feishu card structure rendering. Not a sales-fact source. |
| `中国乘用车销量汇总` / `项目2.1` | `chinese_semantic_processing` | `DeepSeek` | `deepseek-v4-pro` | text sent to DeepSeek if invoked | Chinese sales-text understanding, semantic summarization, and Chinese wording. |
| `JLR 全球及中国销量数据` / `项目 2.2` | `chinese_semantic_understanding` | `DeepSeek` | `deepseek-v4-pro` | query text sent to DeepSeek if invoked | JLR Chinese qualitative sales-text interpretation and Chinese summary wording. |
| `本地图像检索` / `项目 4` | `query_semantic_bridge` | `DeepSeek` | `deepseek-v4-pro` | text query sent to DeepSeek if invoked | Chinese natural-language query understanding for local image retrieval. |
| `本地图像检索` / `项目 4` | `image_indexing` | Volcengine Ark | `doubao-1-5-vision-pro-32k` or `ARK_ENDPOINT_ID` override | image content sent to Ark if invoked | Image-content understanding during indexing. |
| `个人知识库` / `Agent06` | `cross_language_analysis_and_zh_answer` | `DeepSeek` | `deepseek-v4-pro` | question text and retrieved chunks sent to DeepSeek if configured | Cross-language fact extraction, terminology alignment, logic analysis, and direct Chinese suggestions. |
| `个人知识库` / `Agent06` | `english_report_generation` | `codex-base` | local Codex base model route | local runtime route; no source-controlled key | English report generation from DeepSeek analysis and retrieved chunks. |
| `个人知识库` / `Agent06` | `local_semantic_retrieval` | Ollama | `bge-m3` | local only | Embedding generation for ChromaDB semantic retrieval. |

## Key Handling

- Do not store API keys in code.
- Do not print API keys, webhook tokens, app secrets, or access tokens in chat or logs.
- Project-local `.env` files may contain placeholders or runtime-loaded values, but source files should only reference environment variables.
- Reports should state key status as set/unset/placeholder/masked only.

## Update Rule

When a project model route changes, update:

- the project implementation/config;
- the project tests;
- this global model routing record.
